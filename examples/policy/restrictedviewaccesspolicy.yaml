apiVersion: policy.adx.functional.team/v1alpha1
kind: RestrictedViewAccessPolicy
metadata:
  name: pii-restricted
  namespace: data-platform
spec:
  forProvider:
    database: Telemetry
    entity:
      kind: Table
      name: CustomerPII
    enabled: true
  providerConfigRef:
    kind: ProviderConfig
    name: telemetry-prod
