# Installs the provider from the private GHCR package. Create the pull secret
# first (see README.md in this directory).
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-azure-adx
spec:
  package: ghcr.io/functional-team/provider-azure-adx:v0.1.0-rc.1
  packagePullSecrets:
    - name: ghcr-functional-team
