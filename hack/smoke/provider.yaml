# ghcr.io serves this package publicly, so no pull secret is needed. The same
# package sits at xpkg.upbound.io/functional-team/provider-azure-adx with the
# Marketplace extension layers, but pulling from there needs a robot token
# until that repository's pull access is public.
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-azure-adx
spec:
  package: ghcr.io/functional-team/provider-azure-adx:v0.1.0-rc.5
