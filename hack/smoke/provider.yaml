# Both registries are public, so no pull secret is needed. xpkg.upbound.io
# additionally carries the Marketplace extension layers; the same package is at
# ghcr.io/functional-team/provider-azure-adx.
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-azure-adx
spec:
  package: xpkg.upbound.io/functional-team/provider-azure-adx:v0.1.0
